# TLS CMakeLists.txt

IF (NOT LEIOT_ENABLE_TLS)
    RETURN ()
ENDIF (NOT LEIOT_ENABLE_TLS)

LIST (APPEND LEIOT_INCLUDES ${CMAKE_CURRENT_LIST_DIR})

LIST (APPEND LEIOT_DEFINITIONS "-DLEIOT_ENABLE_TLS")
