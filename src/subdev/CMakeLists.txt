# CMakeLists.txt

IF (NOT LEIOT_ENABLE_SUBDEV)
    RETURN ()
ENDIF (NOT LEIOT_ENABLE_SUBDEV)

FILE (GLOB_RECURSE SUBDEV_C_SOURCES "${CMAKE_CURRENT_LIST_DIR}/*.c")

LIST (APPEND LEIOT_INCLUDES ${CMAKE_CURRENT_LIST_DIR})

LIST (APPEND LEIOT_DEFINITIONS
    -DGATEWAY_SUPPORT
    )

LIST (APPEND LEIOT_C_SOURCES ${SUBDEV_C_SOURCES})
