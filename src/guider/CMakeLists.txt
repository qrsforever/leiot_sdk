# Guider CMakeLists.txt

LIST (APPEND LEIOT_INCLUDES
    ${CMAKE_CURRENT_LIST_DIR}
    )

FILE (GLOB_RECURSE GUIDER_C_SOURCES "${CMAKE_CURRENT_LIST_DIR}/*.c")

LIST (APPEND LEIOT_INCLUDES ${CMAKE_CURRENT_LIST_DIR})

LIST (APPEND LEIOT_C_SOURCES ${GUIDER_C_SOURCES})
