# CMakeLists.txt

IF (NOT LEIOT_ENABLE_MQTT)
    RETURN ()
ENDIF (NOT LEIOT_ENABLE_MQTT)

FILE (GLOB_RECURSE MQTT_C_SOURCES "${CMAKE_CURRENT_LIST_DIR}/*.c")

LIST (APPEND LEIOT_INCLUDES ${CMAKE_CURRENT_LIST_DIR})

LIST (APPEND LEIOT_DEFINITIONS "-DLEIOT_ENABLE_MQTT")

LIST (APPEND LEIOT_C_SOURCES ${MQTT_C_SOURCES})
